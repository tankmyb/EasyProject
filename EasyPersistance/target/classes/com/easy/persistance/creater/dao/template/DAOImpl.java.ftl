package ${model.packageName};

import org.springframework.stereotype.Repository;
import ${model.ormPackageName}.${model.baseDAOName};
@Repository
public class ${model.className} extends ${model.baseDAOName} implements ${model.daoName}{
  
}
